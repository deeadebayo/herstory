extends pug-templates/_layout__normal

append startScript
    - var keywordContent = "Some SEO stuff"
    - var pageDescContent = "Here's a page description"
    - var namespace = 'quiz'
    - var avatar = ['Frida with the good hair', 'joan', 'maya']

block content
    block content
    .quiz
        .number
            div
                h2#num
            div
                img#mini(src='public/img/Maya.png' alt='avatar')
        .question
            h3#question 
        .answers
            button.choice(style='vertical-align:middle' onclick="getAnswer('a')") 
                p#a 
            button.choice(style='vertical-align:middle' onclick="getAnswer('b')")
                p#b
            button.choice(style='vertical-align:middle' onclick="getAnswer('c')")
                p#c
            button.choice(style='vertical-align:middle' onclick="getAnswer('d')")
                p#d
        button.button(style='vertical-align:middle')
            span#next NEXT
    script.
        function getAnswer(answer){
        console.log(answer);
        var box = document.getElementsByTagName('button');
        console.log(box.length);
        for (var i=0; i<box.length-1; i++){
            box[i].style.opacity=0.5;
            box[i].style.backgroundColor='#A40E4C';
            box[i].style.borderColor='#A40E4C';
        }
        }
block endScript