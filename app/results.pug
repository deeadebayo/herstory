extends pug-templates/_layout__normal

append startScript
    - var keywordContent = "Some SEO stuff"
    - var pageDescContent = "Here's a page description"
    - var namespace = 'results'

block content
    .results
        h1 Results
        table#resultTable
            tr
                th Question
                th Correct Answer
                th User Answer
            tr
                td#ques hello
                td#answC goodbye goodbye goodbye goodbye goodbye goodbye
                td#answU hello hello hello hello hello hello hello
        script.
            var table = document.getElementById('resultTable');
            console.warn('TABLE LENGTH');
            //- console.warn(table.length());
            for(var)
            var row = table.insertRow();
            var cell1 = row.insertCell(0);
            var cell2 = row.insertCell(1);
            var cell3 = row.insertCell(2);
            cell1.innerHTML = 'LORITA';
            cell2.innerHTML = 'JUAN';
            cell3.innerHTML = 'DEE';
block endScript
    
